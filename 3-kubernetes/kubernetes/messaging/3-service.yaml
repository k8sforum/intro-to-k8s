apiVersion: v1
kind: Service
metadata:
  name: messaging
  namespace: mytravels-default
  labels:
    app: messaging
spec:
  type: ClusterIP
  selector:
    app: messaging
  ports:
    - name: http
      port: 5102
      targetPort: 5102
      protocol: TCP
