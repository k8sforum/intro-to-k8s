apiVersion: v1
kind: Service
metadata:
  name: messaging
  namespace: mytravels-default
  labels:
    app: messaging
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  selector:
    app: messaging
  ports:
    - name: http
      port: 5102
      targetPort: 5102
      protocol: TCP
