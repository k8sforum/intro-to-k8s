apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: mytravels-default
  labels:
    app: web
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
